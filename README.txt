##Legislation as a git repo

This was the result of "spidering" the legislation.govt.nz website each night, and commiting changes as git commit.

It also included historical changes to legislation, read from the legislation.govt.nz XML feed.

This repo stopped updating in 2010.

